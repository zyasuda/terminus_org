# テスト用のプレースホルダー画像

いずれも1×1の透明PNGである。中身は使わない。

`server.cjs`は`MOCK2_DIR/images/`から画像を配信する。ここにファイルが無いと、
ブラウザでの目視確認で404が並び、本物の不具合が埋もれる。

`TAS/data/*.json`が参照するファイル名だけを置いている。参照を増やしたら、
次で作り直す。

```bash
cd TAS && node -e "…"   # 生成手順はコミット 履歴を参照
```
